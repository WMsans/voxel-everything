#pragma once
// RenderOrchestrator — the ~30 pass pointers, downsample pipeline, and
// render/beauty orchestration extracted from VoxelWorld (spec Phase 4).
// Needs GPU/Godot runtime; excluded from the native test target.
namespace godot {
class RenderOrchestrator {};
} // namespace godot
